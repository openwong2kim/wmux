### Changed

- The workspace agent roster now leads with each session's pane title instead
  of the vendor name, so a workspace running several Claude Code sessions no
  longer renders as a column of identical "Claude Code · w2-1xx" rows. The
  vendor moves to the muted trailer next to the pane coordinate, and a session
  whose pane has no title still leads with the vendor name as before.
