### Fixed

- **Claude Code permission prompts now turn the pane to Needs you without the hook.** Claude Code often draws the first frame of a permission dialog by moving the cursor instead of starting a new line. The detector read that row glued to the rows around it and missed the prompt, so on a signals-only install the pane stayed Running while the dialog waited for an answer. (#1500)
