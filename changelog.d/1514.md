### Changed

- **The Welcome dialog and the tour show what wmux tells you.** The sample-task
  card and the first two tour steps used to show pane splitting and adding a
  workspace. They now show Fleet with an agent moving up to Needs you, the
  sidebar flag plus the Fleet board, and a fan-out whose tasks each get their
  own worktree branch. The Claude Code statusline row also plays a short clip of
  the line itself: model, account, context, and 5h / 7d usage.
