### Fixed

- **Push relay secrets require secure transport.** Remote relay URLs must use HTTPS; HTTP remains available for loopback development. Unsafe URLs are refused before any request is sent. (#1538)
