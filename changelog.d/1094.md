### Added

- **A workspace bootstrapped on a remote host can now grow and shrink.** "New workspace on this host" (#1067) used to leave you with a fixed grid of whatever panes already existed — an "Add pane" button and a per-pane close button now let it grow and shrink like a local workspace's own pane controls (#1091).
