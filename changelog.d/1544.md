### Added

- Phone: see paired devices, revoke a device or your own phone, and turn off a device's input permission from the phone. Turning permission on stays on the desktop (`GET /api/devices`, advertised as `deviceManagement`).

### Changed

- Revoking a device and changing its input permission are now audit-logged with who did it (desktop, operator or the device itself).
