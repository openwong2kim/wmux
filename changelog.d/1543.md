### Fixed

- **Separate instances verify CDP ownership.** Startup checks that the debugging endpoint contains this instance’s own window target before reporting it as listening or enabling remote browser automation. A collision now logs a clear failure and leaves automation disabled. (#1543)
