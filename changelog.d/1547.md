### Fixed

- **Verify the pane before accepting shared-host completion notifications.** Notifications now use a unique live thread mapping instead of the task host's inherited pane identity, preventing unrelated status and resume-binding updates. Unmapped or ambiguous notifications are refused and never spooled; manually launched sessions without a managed relay retain terminal detection but no longer receive notify-based completion or resume capture. Cross-instance origin recovery remains unsupported. (#1547)
